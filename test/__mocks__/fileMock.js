# Create the folder structure
mkdir "test\__mocks__"

# Create the file (you can copy this content into a new file)
# fileMock.js content:
echo "module.exports = 'test-file-stub';" > "test\__mocks__\fileMock.js"